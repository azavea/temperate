export const commonEnvironment = {
  // Distance in meters to use for API calculations (roughly 30 mi)
  // Keep this in sync with the CCAPI_DISTANCE variable in settings.py
  apiDistance: 48000,
  actionTextMaxLength: 1024,
  supportEmail: 'support@temperate.io',
};
